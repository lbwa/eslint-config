{
  "packages": ["packages/*"],
  "version": "independent",
  "useWorkspaces": true,
  "npmClient": "yarn",
  "ignoreChanges": ["**/*.md", "**/*.json"],
  "command": {
    "version": {
      "message": "chore(release): publish"
    }
  }
}
